import React from 'react';
import "./Layout.scss";
import Navigation from "../Navigation/Navigation";

const Layout = (props) => {
    return (
        <div>
           <Navigation/>
        </div>
    )
};

export default Layout;